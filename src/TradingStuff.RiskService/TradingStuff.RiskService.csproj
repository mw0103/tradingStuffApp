<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\TradingStuff.Contracts\TradingStuff.Contracts.csproj" />
    <ProjectReference Include="..\TradingStuff.ServiceDefaults\TradingStuff.ServiceDefaults.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- The cover check is defence in depth behind the leg-quantity guard, so it is unreachable
         through Evaluate with the quantities that would break it. Internal keeps it directly
         testable rather than untested. -->
    <InternalsVisibleTo Include="TradingStuff.Tests" />
  </ItemGroup>

</Project>
