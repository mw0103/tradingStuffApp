bin/
obj/
.vs/
.vscode/
.idea/
TestResults/
*.user
*.suo
*.log
.DS_Store
**/node_modules/
**/dist/
# ClientApp's Vite build output — regenerated by the BuildClientApp MSBuild target on every build;
# committing it would just be hashed-filename churn on every rebuild.
src/TradingStuff.ResearchService/wwwroot/

# Mutation testing output
StrykerOutput/

# Earnings C1 study: raw vendor pulls (option chains, daily bar series) are large and re-fetchable;
# derived per-event tables and the frozen universe seed are committed beside them.
data/earnings-c1/raw/

# Agent worktrees (Claude Code isolation); never project content.
.claude/worktrees/
